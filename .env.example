BOT_TOKEN=your_telegram_bot_token_here
REDIS_URL=redis://localhost:6379/0
# SC_PROXY=socks5://user:pass@host:port        # proxy for aiogram → Telegram connection
# SC_PROXIES=socks5://u:p@h1:1080,http://h2:8080  # comma-separated fallback proxies for SC downloads (auto-rotated on IP ban)
# SC_SERVER_IP=89.22.234.79                    # your server's public IP (shown in proxy rotation alerts)
# YM_BOT_TOKEN=your_yandex_music_token  # optional: if set, users can share playlists without their own auth
# POSTGRES_URL=postgresql://postgres:postgres@192.168.1.250:5432/music_bot
# VK_TOKEN=your_vk_kate_mobile_token    # optional: enables VK Music search (run get_vk_token.py to obtain)
